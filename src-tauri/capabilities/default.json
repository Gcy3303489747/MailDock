{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main MailDock window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
